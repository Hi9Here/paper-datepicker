#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../web-component-tester/bin/wct-st" "$@"
  ret=$?
else 
  node  "$basedir/../web-component-tester/bin/wct-st" "$@"
  ret=$?
fi
exit $ret
